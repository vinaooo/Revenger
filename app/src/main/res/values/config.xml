<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- A unique identifier for the app (no spaces or special characters allowed) -->
    <string name="config_id">sth</string>

    <!-- The pretty name for the app -->
    <string name="config_name">Não jogar</string>

    <!-- LibRetro core to fetch for this ROM (excluding _libretro_android.so) -->
    <string name="config_core">picodrive</string>
    <!-- <string name="config_core">picodrive</string> -->

    <!-- Snes = snes9x -->
    <!-- megadrive = genesis_plus_gx -->
    <!-- gb = gambatte -->
    <!-- master = gearsystem (best performance for Master System) -->
    <!-- Master e Mega = picodrive (the best) -->
    
    <!-- rom name, the same name the rom file in res/raw without extension -->
    <string name="config_rom">sth</string>

    <!-- Load ROM directly from memory; larger games should have this disabled -->
    <bool name="config_load_bytes">false</bool>

    <!-- Core variables (key=value separated by commas) -->
    <string name="config_variables">picodrive_overclk68k=+400%,picodrive_overclksz80=+100%</string>

    <!-- Fast forward speed multiplier -->
    <integer name="config_fast_forward_multiplier">2</integer>

    <!-- Fullscreen mode to hide system bars -->
    <bool name="config_fullscreen">true</bool>

    <!-- Orientation modes
        1: Portrait only (ignores system auto-rotate)
        2: Landscape only (ignores system auto-rotate)
        3: Any orientation (respects system auto-rotate setting)

        NOTE: Mode 3 will only rotate if system auto-rotate is enabled.
              If auto-rotate is disabled, the app stays in current orientation.
              Modes 1 and 2 always enforce their orientation regardless of system settings.
              Landscape <-> Portrait transitions can be slow as they rebuild the emulator.
    -->
    <integer name="config_orientation">3</integer>

    <!-- Menu display modes
        0: No menu (disabled)
        1: Menu only via back button
        2: Menu only via SELECT + START combo (virtual and physical)
        3: Menu via both back button and SELECT + START combo

        NOTE: Mode 0 disables all menu functionality,
              use with caution!
    -->
    <integer name="config_menu_mode">3</integer>

    <!-- Should we display a virtual gamepad? -->
    <bool name="config_gamepad">true</bool>

    <!-- Should we vibrate on touch? -->
    <bool name="config_gamepad_haptic">false</bool>

    <!-- Allow multiple presses on action buttons (for combos) -->
    <bool name="config_gamepad_allow_multiple_presses_action">false</bool>

    <!-- GamePad buttons to display -->
    <bool name="config_gamepad_a">true</bool>
    <bool name="config_gamepad_b">true</bool>
    <bool name="config_gamepad_x">false</bool>
    <bool name="config_gamepad_y">false</bool>
    <bool name="config_gamepad_start">true</bool>
    <bool name="config_gamepad_select">true</bool>
    <bool name="config_gamepad_l1">false</bool>
    <bool name="config_gamepad_r1">false</bool>
    <bool name="config_gamepad_l2">false</bool>
    <bool name="config_gamepad_r2">false</bool>

    <!-- Fake buttons (custom mapped buttons) -->
    <bool name="config_show_fake_button_0">false</bool>
    <bool name="config_show_fake_button_1">false</bool>
    <bool name="config_show_fake_button_5">false</bool>
    <bool name="config_show_fake_button_6">false</bool>
    <bool name="config_show_fake_button_7">false</bool>
    <bool name="config_show_fake_button_9">false</bool>
    <bool name="config_show_fake_button_10">false</bool>
    <bool name="config_show_fake_button_11">false</bool>

    <!-- Menu mode for gamepad menu button (☰) -->
    <bool name="config_menu_mode_gamepad">true</bool>

    <!-- Alternative menu modes (deprecated, use config_menu_mode instead) -->
    <bool name="config_menu_mode_back">true</bool>
    <bool name="config_menu_mode_combo">true</bool>

    <!-- Should the left DPAD be replaced with an analog stick? -->
    <bool name="config_left_analog">false</bool>

    <!-- Show performance overlay (FPS, memory, CPU) -->
    <bool name="config_performance_overlay">false</bool>

    <!-- Shader configuration
        "disabled" = No shader applied
        "sharp" = Sharp bilinear filtering
        "crt" = CRT shader
        "lcd" = LCD shader
        "settings" = Allow user to select shader in menu
    -->
    <string name="config_shader">settings</string>
</resources>
