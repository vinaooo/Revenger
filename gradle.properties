# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# ==== GRADLE PERFORMANCE OPTIMIZATIONS ====
# Specifies the JVM arguments used for the daemon process.
org.gradle.jvmargs=-Xmx8192m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -XX:+UseG1GC -XX:MaxMetaspaceSize=1024m

# Enable parallel execution of projects 
org.gradle.parallel=true

# Enable Gradle build cache for faster incremental builds
org.gradle.caching=true

# Enable daemon for faster builds (keeps Gradle process alive)
org.gradle.daemon=true

# Enable configuration cache for faster builds (Gradle 6.6+) - disabled due to prepareCore task
# org.gradle.configuration-cache=true
org.gradle.configuration-cache.problems=warn

# Enable file system watching for better incremental builds (Gradle 7.0+)
org.gradle.vfs.watch=true

# Enable build scan insights (optional)
# org.gradle.console=verbose
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app"s APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Automatically convert third-party libraries to use AndroidX
android.enableJetifier=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Suppress unsupported compileSdk warning for Android 16
android.suppressUnsupportedCompileSdk=36
