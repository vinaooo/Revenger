<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Transparent fill so underlying dialog background shows through -->
    <solid android:color="@color/rm_transparent_color" />
    <stroke
        android:width="@dimen/rm_menu_border_width"
        android:color="@color/rm_divider" />
    <corners android:radius="@dimen/rm_menu_item_corner_radius" />
    <padding android:left="6dp" android:top="6dp" android:right="6dp" android:bottom="6dp" />
</shape>
