package com.vinaooo.revenger.utils

/**
 * Constants for SharedPreferences keys used throughout the application. These are internal
 * configuration keys and should not be user-configurable.
 */
object PreferencesConstants {
    // Frame speed preference key
    const val PREF_FRAME_SPEED = "frame_speed"

    // Audio enabled preference key
    const val PREF_AUDIO_ENABLED = "audio_enabled"
}
