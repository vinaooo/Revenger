<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/rm_background" />
    <corners android:radius="4dp" />
    <stroke
        android:width="@dimen/rm_menu_border_width"
        android:color="@color/rm_divider" />
    <!-- Optional padding so content doesn't touch the stroke -->
    <padding
        android:left="6dp"
        android:top="6dp"
        android:right="6dp"
        android:bottom="6dp" />
</shape>
